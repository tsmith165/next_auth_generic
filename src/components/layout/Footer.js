import styles from "../../../styles/Footer.module.scss"

const Footer = ({}) => {
    return (
        <footer className={styles.footer}>
            Contact Info
        </footer>
    )
}

export default Footer;